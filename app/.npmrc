# Vercel's build ran `npm install` for five minutes on 4 September with
# nothing to install: the audit request to the registry hung until npm gave
# up, and every other build of the day took three seconds. The audit is not
# part of the build and the funding notice is noise, so neither runs here.
audit=false
fund=false
